"use client";

export function FurnIcon({ className }: { id: string; className?: string }) {
  return <div className={className} style={{ width: "100%", height: "100%", background: "#1a1428" }} aria-hidden />;
}
